#Small FreeBASIC program to receive transmitted values over usb2serial uart
Tested on Linux, should work with slight changes on windows.

Otherwise any capable terminal program like putty, minicom, picocom, TeraTerm or the like should work, too. 
Then you have to save a copy of the output for yourself. 